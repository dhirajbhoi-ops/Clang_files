#include<stdio.h>

int main(void)
{
    int number = 3;

    // printf("Enter the number: ");

    // scanf("%d", &number);

    for (int i = 1 ; i <= 4 ; i++)
    {
        // for (k = 1 ; k <= 4;)
        for (int j = 4 ; j <= 1 ; j--)
        {
            printf("%d", j);
        }
        printf("\n");
    }
}